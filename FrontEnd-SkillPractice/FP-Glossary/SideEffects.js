// Side effects(副作用)
// A function or expression is said to have a side effect if apart from returning a value,
// it interacts with (reads from or writes to) external mutable state.
// 如果一个函数或表达式除了返回一个值之外，还与外部可变状态进行交互（读或写），则称它具有副作用。